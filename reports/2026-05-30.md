# Báo cáo ngày 30/05/2026

## Tổng quan

Phiên làm việc tập trung hoàn thiện Admin CMS, Video Player, kết nối DB cho student/guest portal, và fix nhiều bugs.

---

## 1. Admin CMS — Lưu vào DB thật

### Schema mới (prisma db push)
- `Lesson` thêm: `videoUrl`, `zoomUrl`, `azotaUrl`, `azotaDeadline`
- `Course` thêm: `introVideo`

### API routes mới
| Route | Method | Chức năng |
|---|---|---|
| `/api/lessons/[id]` | GET/PUT/DELETE | CRUD lesson từ DB |
| `/api/lessons/[id]/context` | GET | Trả lesson + full course tree |
| `/api/courses/[id]/sections` | POST | Tạo section mới |
| `/api/sections/[id]` | PUT/DELETE | Sửa/xoá section |
| `/api/sections/[id]/chapters` | POST | Tạo chapter mới |
| `/api/chapters/[id]` | PUT/DELETE | Sửa/xoá chapter |
| `/api/chapters/[id]/lessons` | POST | Tạo lesson mới |

### Admin detail page (`/admin/khoa-hoc/[id]`)
- **Viết lại hoàn toàn**: load từ `GET /api/courses/{slug}` thay static lib
- `TabCaiDat`: save thật qua `PUT /api/courses/{slug}`
- `TabChuongBai`: mỗi thao tác add/edit/delete gọi API ngay
- **`apiPost`/`apiPut` helpers**: check `res.ok`, nếu 401 → alert "Phiên đăng nhập hết hạn"
- `introVideo` field thêm vào Cài đặt
- Footer dính đáy: `flex flex-col min-h: calc(100vh - 130px)`

### Admin listing page (`/admin/khoa-hoc`)
- Thiết kế lại: table design, footer dính đáy với `flex-col + flex-1`
- Bỏ pagination hardcode `[1,2,3]`
- URL dùng slug thay numeric adminId

---

## 2. Video Player thật (YouTube embed)

**`student/bai-giang/[lessonId]`** — viết lại hoàn toàn:
- `useEffect` fetch `/api/lessons/{id}/context` → lấy lesson + full course từ DB
- `VideoPlayer` component: nếu URL YouTube → embed iframe; không có → placeholder
- Hỗ trợ: youtube.com/watch, youtu.be, youtube.com/embed, youtube.com/shorts
- Sidebar `CourseOutline` dùng DB sections thay static lib
- Breadcrumb dùng `dbCourse.name` thay static data

---

## 3. Student Portal — Load từ DB

### `student/hoc-tap`
- `openCourse(courseId)` fetch `GET /api/courses/{id}` khi click vào khoá học
- `toEnrolledCourse(full, completedIds)` nhận full data với sections thật
- `c.lessons` (stored count) hiện trên card thay vì tính từ sections rỗng
- Loading spinner khi đang fetch

### `POST /api/chapters/[id]/lessons` sync count
- Sau khi tạo lesson → `course.lessons += 1`
- Sau khi xoá lesson → `course.lessons -= 1`

---

## 4. Guest Portal — Load từ DB

### `guest/khoa-hoc/[slug]`
- Fetch `GET /api/courses/{slug}` thay static COURSES array
- Curriculum hiện từ DB sections/chapters/lessons
- **Free lessons clickable**: `<Link href="/student/bai-giang/{id}">` + nút ▶ Xem
- **Video giới thiệu**: nếu `course.introVideo` có YouTube URL → embed iframe thật

---

## 5. Bugs đã fix

| # | Bug | Fix |
|---|---|---|
| 1 | Admin listing table ẩn (min-w-900px) | Đổi key sang slug, bỏ column ảnh bìa |
| 2 | `sections.map` React key warning | Fallback `section.id \|\| \`s-${si}\`` |
| 3 | `TabChuongBai` không check `res.ok` → title = undefined khi 401 | `apiPost/apiPut` helpers với check + alert |
| 4 | `useEnrollments` chỉ trả courseIds, không có curriculum | Fetch full course khi click card |
| 5 | Guest curriculum hardcode | Fetch từ DB |
| 6 | `course.lessons` count không sync | Increment/decrement khi thêm/xoá lesson |
| 7 | `proxy.ts` named export → default export | Fix theo Next.js 16 docs |
| 8 | Dọn dẹp 8 khoá học cũ → 1 khoá test | Xoá qua API, tạo mới với 1 lesson YouTube |

---

## 6. Xác nhận bug session này đã tìm ra nhưng chưa fix hết

Xem phần **Còn lại** bên dưới.

---

## Còn lại (chuyển sang phiên 31/05)

### Bug cần fix ngay

**1. "Failed to update course" khi lưu Cài đặt khoá học**
- **Nguyên nhân**: Prisma client cũ không biết field `introVideo` mới thêm
- **Fix**: Restart dev server (kill node) để reload Prisma client với field mới
- **Hoặc**: Sửa PUT route `api/courses/[id]` để pick explicit fields thay `data: body`

**2. Guest click bài học free → nhảy vào trang admin**
- **Nguyên nhân**: Admin đang login, click `/student/bai-giang/{id}`, student layout `AuthGuard` có `requiredRole="student"`, admin bị redirect về `/admin`
- **Fix**: Cho phép admin cũng xem student pages (preview). Sửa `AuthGuard` hoặc student layout để không redirect admin sang `/admin` khi truy cập student routes

**3. Course Catalog: phải click "Xem chi tiết" mới vào được, muốn click anywhere trên card**
- **Fix**: Wrap toàn bộ card content trong `<Link href="/khoa-hoc/{slug}">` hoặc thêm `onClick={() => router.push(...)}`
- File: `src/app/(guest)/khoa-hoc/page.tsx` → `CourseCard` component

### Tính năng còn lại (từ danh sách trước)

| # | Việc | Ưu tiên |
|---|---|---|
| 1 | Video player: Bunny CDN / playlist | Thấp |
| 2 | Session management: xem/thu hồi thiết bị | Thấp |
| 3 | Email notifications: đề thi mới, nhắc lịch | Thấp |

---

## Tài khoản demo

| Role | Email | Mật khẩu |
|---|---|---|
| Super Admin | `admin@tsix.vn` | `admin123` |
| Content Admin | `content@tsix.vn` | `content123` |
| Học viên | `student@tsix.vn` | `123456` |

URL: http://localhost:3000

---

## Trạng thái khoá học test

- Course ID: `khoa-hoc-thu-nghiem`
- 1 section → 1 chapter → 1 lesson ("Bai 1: Xin chao the gioi" với YouTube URL)
- `student@tsix.vn` enrolled
- `course.lessons = 1` (sync với DB)

---

---

# PHIÊN LÀM VIỆC TIẾP THEO — Cùng ngày 30/05/2026 (buổi chiều/tối)

## Tổng quan phiên 2

Tiếp tục từ phiên sáng: audit toàn bộ `/admin/khoa-hoc/`, hoàn thiện `/admin/thi-thu/`, kết nối data thật cho `/admin/vinh-danh/` và leaderboard, implement slide-over drawers, API-level permissions.

---

## 7. Admin /khoa-hoc/ — Audit & hoàn thiện toàn diện

### Danh mục khoá học (`/admin/khoa-hoc/danh-muc`)
- Rewrite từ `INITIAL_CATEGORIES` mock → load từ DB thật (`GET /api/categories`)
- `PUT /api/categories` — đổi tên danh mục → cập nhật tất cả courses trong DB
- Fix encoding bug: category `Đ` bị lưu thành U+FFFD (`ï¿½GNL HSA`) → script fix
- Drawer: `visibility:hidden + pointerEvents:none + translateX(110%)` pattern
- Xóa `autoFocus` trên input → tránh browser scroll hijack
- Xóa cột "Trạng thái" (statusMap local-only, vô nghĩa)

### Code cleanup `/admin/khoa-hoc/`
- Tạo `src/components/Toggle.tsx` shared — xóa 3 local duplicates
- Tạo `src/lib/slug.ts` shared — xóa 2 local `toSlug` definitions
- Fix `TabId` type: xóa dead `"lien-quan"`
- `[id]/page.tsx` và `tao-moi/page.tsx`: load categories từ `/api/categories` thay hardcode
- Xóa `const CATEGORIES = ADMIN_CATEGORIES` alias vô nghĩa

### Course status sync toàn hệ thống
- `GET /api/courses` role-based: guest → `status:true`, student → active + enrolled, admin → `?all=1`
- `GET /api/courses/[id]`: hidden course → 404 với guest/student chưa enroll
- Admin listing dùng `useCourses({ all: "1" })`, public pages dùng `useCourses()`
- `apiFetch` thêm `credentials: "same-origin"` explicit

---

## 8. API-level Permission Enforcement

### Files mới/sửa
- `src/lib/permissions.ts` (NEW) — shared permission constants, dùng được server + client
- `src/lib/auth-guard.ts` — thêm `requirePermission(permission)` function
- `src/contexts/AuthContext.tsx` — import từ permissions.ts, bỏ duplicate definitions

### 17 API routes cập nhật
| Permission | Routes |
|---|---|
| `MANAGE_COURSES` | POST/PUT/DELETE courses, PUT categories, GET students-per-course |
| `MANAGE_CURRICULUM` | sections, chapters, lessons, exams CRUD |
| `VIEW_STUDENTS` | GET /api/admin/students |
| `MANAGE_STUDENTS` | POST /api/admin/students, POST/DELETE /api/admin/enrollments |
| `VIEW_REVENUE` | GET /api/admin/analytics |

---

## 9. Slide-over Drawers (tạo + sửa)

### Pattern chung
```
visibility:hidden + pointerEvents:none + translateX(110%) khi đóng
Escape key + backdrop click → close
Sau save: onCreated()/onSaved() → refetch() → list tự update
```

### `/admin/thi-thu`
- `CreateExamDrawer` — tạo đề thi mới từ phải, có preview mã đề auto
- `EditExamDrawer` — sửa từng đề thi từ phải, pre-populate form
- `/admin/thi-thu/tao-moi` và `[id]` → redirect về listing
- Xóa "Tạo đề thi mới" khỏi sidebar

### `/admin/khoa-hoc`
- `CreateCourseDrawer` — tạo khoá học mới từ phải, full form
- `/admin/khoa-hoc/tao-moi` → redirect về listing

---

## 10. Admin /thi-thu/ — Audit & fix toàn diện

| Vấn đề | Fix |
|---|---|
| `POST /api/exams` không có allowlist | Thêm allowlist, block unknown fields |
| `toInputDate("")` crash | Guard `if (!d \|\| !d.includes("/")) return ""` |
| `examData.ts` 80 dòng dead code | Xóa EXAMS mock, MockExam, countByStatus — giữ ExamStatus |
| `PUT /api/exams/[id]` ghi đè id/code/participants | Allowlist — block read-only fields |
| Category dropdown hardcode ADMIN_CATEGORIES | Load từ DB exams thật |
| Categories không nhất quán (listing vs create vs edit) | Đồng nhất từ examCategories API |
| SkeletonTable import thừa | Xóa, thêm loading skeleton thật |
| Toggle local trong `[id]` | Thay bằng shared Toggle component |
| "Cải thiện" sort/badge với monthDelta=0 | Xóa sort, hiện "Cần dữ liệu lịch sử" |
| `TABS` trong component | Chuyển ra ngoài |
| API honor-leaderboard thiếu try/catch | Thêm error handling |

---

## 11. Admin /vinh-danh/ + Bang xep hang — Real Data

### API mới
- `GET /api/honor-leaderboard` — tính GPA từ DB thật:
  ```
  completion = completedLessons / totalEnrolledLessons × 100
  gpa = (completion/10 × 0.4) + (bestExamScore/totalPoints×10 × 0.6)
  ```
- `GET /api/leaderboard?category=` — thêm filter theo exam category

### Files mới
- `src/components/GpaBar.tsx` — shared GPA progress bar (xóa 2 local duplicates)
- `src/lib/honorData.ts` — xóa HONOR_STUDENTS mock (8 người), giữ types + BADGE_RULES

### Kết nối data thật
| Trước | Sau |
|---|---|
| HONOR_STUDENTS hardcode 8 người | Load từ `/api/honor-leaderboard` |
| MY_HONOR_RANK = 12 cố định | Tìm từ ranked list API |
| "2,847 thí sinh" hardcode | Đếm từ API response |
| Filter thi-thu UI-only | Gọi `/api/leaderboard?category=...` |
| allLeaderboard mock 10 người | Load từ `/api/leaderboard` |
| examTypes hardcode | Dùng COURSE_CATEGORIES |

### Import điểm thi (honest)
- Thay fake `setTimeout → "247 kết quả"` bằng warning "Tính năng đang phát triển"
- Lịch sử import hardcode → bỏ

---

## 12. Trạng thái Admin Sidebar (cuối ngày)

| Section | Trạng thái |
|---|---|
| Khóa học (CMS) | ✅ Hoàn thiện |
| Thi thử | ✅ Hoàn thiện |
| Bảng vinh danh | ✅ Hoàn thiện |
| Dashboard `/admin` | ❌ Chưa làm — toàn bộ mock |
| Hồ sơ học sinh | ⚠️ Có API nhưng chưa audit |
| Doanh thu | ⚠️ Có API nhưng chưa audit |
| Quản trị viên | ⚠️ Có API nhưng chưa audit |

---

## Files tạo mới trong phiên 2

| File | Mục đích |
|---|---|
| `src/lib/permissions.ts` | Shared permission constants |
| `src/lib/slug.ts` | Shared toSlug utility |
| `src/components/Toggle.tsx` | Shared toggle switch |
| `src/components/GpaBar.tsx` | Shared GPA bar |
| `src/app/api/categories/route.ts` | GET/PUT categories từ courses |
| `src/app/api/honor-leaderboard/route.ts` | GET honor board từ DB |

---

## Còn lại (phiên tiếp theo)

**Ưu tiên:**
1. Dashboard — mock hoàn toàn, cần kết nối DB
2. Audit Hồ sơ học sinh / Doanh thu / Quản trị viên

**Backlog:**
- Historical monthDelta cho honor board
- Badge persistence (DB)
- CSV import exam results
