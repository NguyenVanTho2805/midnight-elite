# Báo cáo công việc — 28/05/2026

## Tổng quan
Phiên làm việc tập trung vào đồng bộ thiết kế, bổ sung tính năng và liên kết các chức năng giữa các role trong nền tảng TSIX Education.

---

## Các thay đổi đã thực hiện

### 1. Đồng bộ AIChatWidget sang Guest & Home
- Thêm `AIChatWidget` vào `(guest)/layout.tsx` → hiển thị trên toàn bộ trang guest
- Thêm `AIChatWidget` vào `src/app/page.tsx` (trang chủ tự render Navbar/Footer riêng)
- Admin layout giữ nguyên (dark admin panel, không cần chatbot học sinh)

### 2. Thêm Lịch sử mua hàng (student)
- Tạo trang `/student/lich-su-mua-hang/page.tsx` với 3 đơn hàng mock
- Thêm link "Lịch sử mua hàng" vào UserPanel dropdown (giữa Giỏ hàng và Đăng xuất)
- Icon màu vàng cam, badge trạng thái: Thành công / Đang xử lý / Đã hoàn tiền

### 3. Đồng bộ giao diện card khóa học Guest `/khoa-hoc`
- Rewrite toàn bộ card design từ flat → gradient thumbnail giống home page và student portal
- Thêm các trường: `shortTitle`, `teacher`, `teacherAvatar`, `openDate`, `types`, `tagColor`
- `categoryTheme` với gradient màu theo danh mục (xanh/xanh lá/tím/cam)
- Badge Video/Live, category strip vàng, teacher avatar

### 4. Bảng xếp hạng & Tin tức trong Student Portal
- Tạo `/student/bang-xep-hang/page.tsx`:
  - Banner "Vị trí của bạn: #47" cá nhân hóa theo user đang đăng nhập
  - 2 tab: Thi thử (top 10 + podium) & Vinh danh (GPA, điểm thi, tiến độ, badge)
  - Highlight hàng của học viên trong bảng full
- Tạo `/student/tin-tuc/page.tsx`:
  - Search + filter 5 danh mục
  - Bài nổi bật + grid bài viết
- Thêm "Xếp hạng" và "Tin tức" vào desktop nav + mobile bottom nav (5 items)

### 5. Liên kết khóa học trong Dashboard
- Sync IDs: `"KH001"→"hsa-tron-goi"`, `"KH003"→"toan-12"` khớp với hoc-tap
- Sync `nextLessonId`: `"b02-nt01"`, `"t-b01"` dùng đúng ID bài học thực
- Tiêu đề khóa học → Link `/student/hoc-tap?course=<id>`
- Thêm nút "Xem lộ trình đầy đủ" → `/student/hoc-tap?course=<id>`
- Thêm "Xem tất cả →" header link
- Hoc-tap đọc `?course=<id>` qua `useSearchParams` → auto-open CurriculumView
- Wrap trong `Suspense` theo yêu cầu Next.js App Router

---

## Files đã thay đổi / tạo mới

| File | Hành động |
|------|-----------|
| `src/app/(guest)/layout.tsx` | Thêm AIChatWidget |
| `src/app/page.tsx` | Thêm AIChatWidget |
| `src/app/(student)/layout.tsx` | Thêm link Lịch sử mua hàng trong UserPanel |
| `src/app/(student)/student/lich-su-mua-hang/page.tsx` | **Tạo mới** |
| `src/app/(guest)/khoa-hoc/page.tsx` | Rewrite card design → gradient thumbnail |
| `src/app/(student)/student/bang-xep-hang/page.tsx` | **Tạo mới** |
| `src/app/(student)/student/tin-tuc/page.tsx` | **Tạo mới** |
| `src/components/StudentBottomNav.tsx` | Thêm Xếp hạng + Tin tức (5 items) |
| `src/app/(student)/student/page.tsx` | Sync IDs, link courses, thêm CTA |
| `src/app/(student)/student/hoc-tap/page.tsx` | Thêm useSearchParams + Suspense |

---

## Trạng thái TypeScript
✅ `npx tsc --noEmit` — **0 lỗi**

---

## Ghi chú
- Backend (Prisma + PostgreSQL, NextAuth, VNPay, Claude API) chưa triển khai
- Tất cả dữ liệu đang dùng mock data
- Dev server chạy tại `http://localhost:3000`
